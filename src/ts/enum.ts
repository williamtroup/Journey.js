export const enum Char {
    empty = "",
    space = " ",
}

export const enum KeyCode {
    escape = "Escape",
    left = "ArrowLeft",
    up = "ArrowUp",
    right = "ArrowRight",
    down = "ArrowDown",
}